#include <stdio.h>
#include "main.h"

/**
 * main - function that prints the proggram name
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: always 0 (success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
